<!--
  低速智能车 NavigateToPose 安全恢复树。

  正常 DWB 导航仍禁止倒车；恢复时先清图，再尝试短距离倒车，
  然后分别尝试左60°和右60°。第二次旋转从左60°跨过原始方向到右60°，
  避免“左90°再右90°”回到原地却没有换方向的无效恢复。
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="4" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"
              server_timeout="2000"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <!-- 香橙派负载高时 20ms 不足以等待 FollowPath 应答，会与恢复动作竞态。 -->
        <FollowPath path="{path}" controller_id="FollowPath" server_timeout="2000"/>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <Sequence name="ClearingActions">
            <ClearEntireCostmap name="ClearLocalCostmap-Subtree"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Subtree"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          <BackUp name="ShortBackUp" backup_dist="0.10" backup_speed="0.05"/>
          <Spin name="SpinLeft60" spin_dist="1.0472"/>
          <Spin name="SpinAcrossToRight60" spin_dist="-2.0944"/>
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
